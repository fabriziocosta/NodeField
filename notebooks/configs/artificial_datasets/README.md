# Artificial dataset configs

Generated artificial cycle/path/star dataset YAML files belong in this
directory. Notebook workflows pass this directory through
`save_config_dir`, and generated YAML is ignored by Git so it does not clutter
the notebook root or the repository.
